r2dbc:
  datasources:
    default:
      schema-generate: CREATE_DROP
      dialect: POSTGRES
      url: r2dbc:tc:postgresql:///glomgold?TC_IMAGE_TAG=12
      driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver

liquibase:
  enabled: false

logger:
  levels:
#    io.micronaut.http.client: TRACE
#    io.micronaut.data.query: TRACE